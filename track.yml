name: Track leaderboard

on:
  schedule:
    # Runs at the top of every hour (UTC). GitHub may delay it a few minutes
    # under load; that's fine — the tracker uses real timestamps, not slots.
    - cron: "0 * * * *"
  workflow_dispatch: {}   # lets you trigger a run manually from the Actions tab

# Allow the workflow to push the updated history.json back to the repo.
permissions:
  contents: write

concurrency:
  group: track-leaderboard
  cancel-in-progress: false

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch and record a snapshot
        run: python fetch_leaderboard.py

      - name: Commit if anything changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add history.json latest_raw.json
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "snapshot: $(date -u +'%Y-%m-%dT%H:%MZ')"
            git push
          fi
